name: PR Handler

on:
  pull_request_target:

permissions:
  contents: write
  pull-requests: write

jobs:
  handle:
    runs-on: ubuntu-latest
    steps:
      - name: Echo PR title (vulnerable to script injection)
        run: |
          echo "Title is ${{ github.event.pull_request.title }}"
